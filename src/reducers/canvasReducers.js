// import { DRAW_LINE } from "../actions/types";

const initialState = {
  drawables: [],
  newDrawable: []
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
